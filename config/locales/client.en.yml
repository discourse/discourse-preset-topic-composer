en:
  site_settings:
    button_types: "Launch the editor to define various `new topic` buttons"
    hide_new_topic_button_on_top_menu: "Hide 'New Topic' button on frontpage and top menu pages. Uncheck to display the button on all pages."
    hide_unmatched_composer_presets: "Hide the composer presets that are NOT highlighted as recommendation."
    discourse_preset_topic_composer_enabled: "Enable preset topic composer"

  admin_js:
    admin:
      site_settings:
        categories:
          discourse_preset_topic_composer: "Preset topic composer"

  js:
    discourse_preset_topic_composer:
      placeholder: placeholder
    composer:
      select: "Select "
    dialog:
      error_message: "Please fill required fields"
