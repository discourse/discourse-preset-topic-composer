{{#if currentUser}}

  {{#each this.tagGroupList as |tagGroupOption|}}
    <div>
      {{tagGroupOption.tagGroup}}:
    </div>
    {{#if tagGroupOption.multi }}
      {{tag-group-multiselect tagGroupOption=tagGroupOption}}
    {{else}}
      {{tag-group-combobox tagGroupOption=tagGroupOption}}
    {{/if}}
  {{/each}}

{{/if}}
