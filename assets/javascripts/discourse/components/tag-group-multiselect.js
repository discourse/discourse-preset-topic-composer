import MultiSelectComponent from "select-kit/components/multi-select";
import { tagGroupOptions } from "../../common/tag_group_options";

export default MultiSelectComponent.extend({
  ...tagGroupOptions
});
